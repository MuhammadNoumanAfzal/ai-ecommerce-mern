import React from "react";
import { SheetContent, SheetHeader } from "../ui/sheet";

const CartWrapper = () => {
  return (
    <SheetContent className="sm:max-w-md">
      <SheetHeader>Your dfngrdogo Cart</SheetHeader>
    </SheetContent>
  );
};

export default CartWrapper;
